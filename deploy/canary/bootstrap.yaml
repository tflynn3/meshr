apiVersion: batch/v1
kind: Job
metadata:
  name: canary-store-bootstrap
  labels:
    app.kubernetes.io/name: store-bootstrap-canary
    app.kubernetes.io/part-of: meshr
  annotations:
    kustomize.toolkit.fluxcd.io/force: enabled
spec:
  backoffLimit: 6
  activeDeadlineSeconds: 600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: store-bootstrap-canary
        app.kubernetes.io/part-of: meshr
    spec:
      serviceAccountName: meshr-bootstrap-canary
      restartPolicy: OnFailure
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: store-bootstrap
          image: ${MESHR_YAML_QUOTE}${EVENT_PLANE_IMAGE}${MESHR_YAML_QUOTE}
          imagePullPolicy: IfNotPresent
          args: ["production-bootstrap"]
          envFrom:
            - configMapRef: {name: meshr-runtime-canary}
          env:
            - name: MESHR_RELEASE_SHA
              value: ${MESHR_YAML_QUOTE}${MESHR_RELEASE_SHA}${MESHR_YAML_QUOTE}
            # Include the selected databases in the Job template so a
            # same-image restore reruns bootstrap against the new stores.
            - name: MESHR_FIRESTORE_DATABASE
              value: ${MESHR_YAML_QUOTE}${MESHR_FIRESTORE_DATABASE}${MESHR_YAML_QUOTE}
            - name: MESHR_TOPOLOGY_FIRESTORE_DATABASE
              value: ${MESHR_YAML_QUOTE}${MESHR_TOPOLOGY_FIRESTORE_DATABASE}${MESHR_YAML_QUOTE}
            - name: MESHR_FORCE_PROJECTION_BOOTSTRAP_SCAN
              value: ${MESHR_YAML_QUOTE}${MESHR_FORCE_PROJECTION_BOOTSTRAP_SCAN}${MESHR_YAML_QUOTE}
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 500m, memory: 256Mi}
