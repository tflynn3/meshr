apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-api-canary
  annotations:
    iam.gke.io/gcp-service-account: ${MESHR_YAML_QUOTE}meshr-api-canary@${GCP_PROJECT_ID}.iam.gserviceaccount.com${MESHR_YAML_QUOTE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-bootstrap-canary
  annotations:
    iam.gke.io/gcp-service-account: ${MESHR_YAML_QUOTE}meshr-bootstrap-canary@${GCP_PROJECT_ID}.iam.gserviceaccount.com${MESHR_YAML_QUOTE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-live-gateway-canary
  annotations:
    iam.gke.io/gcp-service-account: ${MESHR_YAML_QUOTE}meshr-live-gateway-canary@${GCP_PROJECT_ID}.iam.gserviceaccount.com${MESHR_YAML_QUOTE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-web-canary
automountServiceAccountToken: false
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-ingest-canary
  annotations:
    iam.gke.io/gcp-service-account: ${MESHR_YAML_QUOTE}meshr-ingest-canary@${GCP_PROJECT_ID}.iam.gserviceaccount.com${MESHR_YAML_QUOTE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-topology-materializer-canary
  annotations:
    iam.gke.io/gcp-service-account: ${MESHR_YAML_QUOTE}meshr-topology-canary@${GCP_PROJECT_ID}.iam.gserviceaccount.com${MESHR_YAML_QUOTE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-moderation-worker-canary
  annotations:
    iam.gke.io/gcp-service-account: ${MESHR_YAML_QUOTE}meshr-moderation-canary@${GCP_PROJECT_ID}.iam.gserviceaccount.com${MESHR_YAML_QUOTE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-moderation-screening-worker-canary
  annotations:
    iam.gke.io/gcp-service-account: ${MESHR_YAML_QUOTE}meshr-mod-screening-canary@${GCP_PROJECT_ID}.iam.gserviceaccount.com${MESHR_YAML_QUOTE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-audit-worker-canary
  annotations:
    iam.gke.io/gcp-service-account: ${MESHR_YAML_QUOTE}meshr-audit-canary@${GCP_PROJECT_ID}.iam.gserviceaccount.com${MESHR_YAML_QUOTE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: meshr-notification-worker-canary
  annotations:
    iam.gke.io/gcp-service-account: ${MESHR_YAML_QUOTE}meshr-notifications-canary@${GCP_PROJECT_ID}.iam.gserviceaccount.com${MESHR_YAML_QUOTE}
