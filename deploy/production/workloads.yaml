apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  labels: {app.kubernetes.io/name: api, app.kubernetes.io/part-of: meshr}
spec:
  replicas: 2
  selector:
    matchLabels: {app.kubernetes.io/name: api}
  template:
    metadata:
      labels: {app.kubernetes.io/name: api, app.kubernetes.io/part-of: meshr}
    spec:
      serviceAccountName: meshr-api
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      terminationGracePeriodSeconds: 30
      containers:
        - name: api
          image: ${API_IMAGE}
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef: {name: meshr-runtime}
          env:
            - name: MESHR_PORT
              value: "8787"
            # Keep the selected databases in the pod template so a protected
            # restore rolls every replica even when the image is unchanged.
            - name: MESHR_FIRESTORE_DATABASE
              value: ${MESHR_FIRESTORE_DATABASE}
            - name: MESHR_TOPOLOGY_FIRESTORE_DATABASE
              value: ${MESHR_TOPOLOGY_FIRESTORE_DATABASE}
            - name: MESHR_DATABASE_CUTOVER_MODE
              value: ${MESHR_DATABASE_CUTOVER_MODE}
            - name: MESHR_CUTOVER_VALIDATION_MESH_ID
              value: ${MESHR_CUTOVER_VALIDATION_MESH_ID}
            - name: MESHR_CUTOVER_VALIDATION_BINDING_ID
              value: ${MESHR_CUTOVER_VALIDATION_BINDING_ID}
            - name: MESHR_CUTOVER_VALIDATION_AGENT_ID
              value: ${MESHR_CUTOVER_VALIDATION_AGENT_ID}
            - name: MESHR_CUTOVER_VALIDATION_SESSION_ID
              value: ${MESHR_CUTOVER_VALIDATION_SESSION_ID}
            - name: MESHR_EXPECTED_AUTHORITY_BOOTSTRAP_ID
              value: ${MESHR_EXPECTED_AUTHORITY_BOOTSTRAP_ID}
            - name: MESHR_IDENTITY_API_KEY_FILE
              value: /var/run/secrets/meshr/identity-api-key
            - name: MESHR_MODERATION_AUTHORITY_TOKEN_FILE
              value: /var/run/secrets/meshr-moderation/moderation-authority-token
            - name: MESHR_RENEWAL_RECOVERY_SECRET_FILE
              value: /var/run/secrets/meshr/renewal-recovery
            - name: MESHR_RENEWAL_RECOVERY_SECRET_PREVIOUS_FILE
              value: /var/run/secrets/meshr/renewal-recovery-previous
            - name: MESHR_INVITATION_PEPPER_FILE
              value: /var/run/secrets/meshr/invitation-pepper
            - name: MESHR_INVITATION_PEPPER_PREVIOUS_FILE
              value: /var/run/secrets/meshr/invitation-pepper-previous
            # Explicitly templated so Flux changes to cost protection trigger
            # a Deployment rollout; envFrom alone would leave old pods live.
            - name: MESHR_COST_PROTECTION_MODE
              value: ${MESHR_COST_PROTECTION_MODE}
            # Keep the promoted revision in the pod template so a digest-only
            # release or retry rolls API replicas that report /healthz.
            - name: MESHR_RELEASE_SHA
              value: ${MESHR_RELEASE_SHA}
          ports: [{name: http, containerPort: 8787}]
          readinessProbe:
            httpGet: {path: /readyz, port: http}
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet: {path: /healthz, port: http}
            initialDelaySeconds: 15
            periodSeconds: 20
          volumeMounts:
            - {name: meshr-api-secrets, mountPath: /var/run/secrets/meshr, readOnly: true}
            - {name: meshr-moderation-authority-secrets, mountPath: /var/run/secrets/meshr-moderation, readOnly: true}
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          resources:
            requests: {cpu: 250m, memory: 256Mi}
            limits: {cpu: "1", memory: 512Mi}
      volumes:
        - name: meshr-api-secrets
          csi:
            driver: secrets-store-gke.csi.k8s.io
            readOnly: true
            volumeAttributes: {secretProviderClass: meshr-api-secrets}
        - name: meshr-moderation-authority-secrets
          csi:
            driver: secrets-store-gke.csi.k8s.io
            readOnly: true
            volumeAttributes: {secretProviderClass: meshr-moderation-authority-secrets}
---
apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  selector: {app.kubernetes.io/name: api}
  ports: [{name: http, port: 8787, targetPort: http}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: {name: api}
spec:
  minAvailable: 1
  selector: {matchLabels: {app.kubernetes.io/name: api}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  labels: {app.kubernetes.io/name: web, app.kubernetes.io/part-of: meshr}
spec:
  replicas: 2
  selector: {matchLabels: {app.kubernetes.io/name: web}}
  template:
    metadata: {labels: {app.kubernetes.io/name: web, app.kubernetes.io/part-of: meshr}}
    spec:
      serviceAccountName: meshr-web
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: web
          image: ${WEB_IMAGE}
          imagePullPolicy: IfNotPresent
          ports: [{name: http, containerPort: 8080}]
          readinessProbe: {httpGet: {path: /web-healthz, port: http}}
          livenessProbe: {httpGet: {path: /web-healthz, port: http}, initialDelaySeconds: 10}
          securityContext: {runAsNonRoot: true, allowPrivilegeEscalation: false, readOnlyRootFilesystem: true}
          resources: {requests: {cpu: 20m, memory: 32Mi}, limits: {cpu: 200m, memory: 128Mi}}
---
apiVersion: v1
kind: Service
metadata: {name: web}
spec:
  selector: {app.kubernetes.io/name: web}
  ports: [{name: http, port: 8080, targetPort: http}]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingest
  labels: {app.kubernetes.io/name: ingest, app.kubernetes.io/part-of: meshr}
spec:
  replicas: 2
  selector: {matchLabels: {app.kubernetes.io/name: ingest}}
  template:
    metadata: {labels: {app.kubernetes.io/name: ingest, app.kubernetes.io/part-of: meshr}}
    spec:
      serviceAccountName: meshr-ingest
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: ingest
          image: ${EVENT_PLANE_IMAGE}
          args: ["ingest"]
          envFrom: [{configMapRef: {name: meshr-runtime}}]
          env:
            - name: MESHR_INTERNAL_TOKEN_FILE
              value: /var/run/secrets/meshr/internal-token
            - name: MESHR_FIRESTORE_DATABASE
              value: ${MESHR_FIRESTORE_DATABASE}
            - name: MESHR_TOPOLOGY_FIRESTORE_DATABASE
              value: ${MESHR_TOPOLOGY_FIRESTORE_DATABASE}
          ports: [{name: http, containerPort: 8080}]
          readinessProbe: {httpGet: {path: /readyz, port: http}}
          livenessProbe: {httpGet: {path: /healthz, port: http}, initialDelaySeconds: 15}
          securityContext: {runAsNonRoot: true, allowPrivilegeEscalation: false, readOnlyRootFilesystem: true}
          resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: 500m, memory: 256Mi}}
          volumeMounts:
            - {name: meshr-event-secrets, mountPath: /var/run/secrets/meshr, readOnly: true}
      volumes:
        - name: meshr-event-secrets
          csi:
            driver: secrets-store-gke.csi.k8s.io
            readOnly: true
            volumeAttributes: {secretProviderClass: meshr-event-secrets}
---
apiVersion: v1
kind: Service
metadata: {name: ingest}
spec:
  selector: {app.kubernetes.io/name: ingest}
  ports: [{name: http, port: 8080, targetPort: http}]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: ingest}
spec:
  minReplicas: 2
  maxReplicas: 3
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: ingest}
  metrics:
    - type: Resource
      resource: {name: cpu, target: {type: Utilization, averageUtilization: 65}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: topology-materializer
  labels: {app.kubernetes.io/name: topology-materializer, app.kubernetes.io/part-of: meshr}
spec:
  replicas: 1
  selector: {matchLabels: {app.kubernetes.io/name: topology-materializer}}
  template:
    metadata: {labels: {app.kubernetes.io/name: topology-materializer, app.kubernetes.io/part-of: meshr}}
    spec:
      serviceAccountName: meshr-topology-materializer
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: materializer
          image: ${EVENT_PLANE_IMAGE}
          args: ["materializer", "topology"]
          envFrom: [{configMapRef: {name: meshr-runtime}}]
          env:
            - name: MESHR_FIRESTORE_DATABASE
              value: ${MESHR_FIRESTORE_DATABASE}
            - name: MESHR_TOPOLOGY_FIRESTORE_DATABASE
              value: ${MESHR_TOPOLOGY_FIRESTORE_DATABASE}
            - name: MESHR_EXPECTED_AUTHORITY_BOOTSTRAP_ID
              value: ${MESHR_EXPECTED_AUTHORITY_BOOTSTRAP_ID}
          ports: [{name: http, containerPort: 8080}]
          readinessProbe: {httpGet: {path: /readyz, port: http}}
          livenessProbe: {httpGet: {path: /healthz, port: http}, initialDelaySeconds: 15}
          securityContext: {runAsNonRoot: true, allowPrivilegeEscalation: false, readOnlyRootFilesystem: true}
          resources: {requests: {cpu: 150m, memory: 192Mi}, limits: {cpu: "1", memory: 512Mi}}
---
apiVersion: v1
kind: Service
metadata: {name: topology-materializer}
spec:
  selector: {app.kubernetes.io/name: topology-materializer}
  ports: [{name: http, port: 8080, targetPort: http}]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: topology-materializer}
spec:
  minReplicas: 1
  maxReplicas: 3
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: topology-materializer}
  metrics:
    - type: Resource
      resource: {name: cpu, target: {type: Utilization, averageUtilization: 65}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: moderation-worker
  labels: {app.kubernetes.io/name: moderation-worker, app.kubernetes.io/part-of: meshr}
spec:
  replicas: 1
  selector: {matchLabels: {app.kubernetes.io/name: moderation-worker}}
  template:
    metadata: {labels: {app.kubernetes.io/name: moderation-worker, app.kubernetes.io/part-of: meshr}}
    spec:
      serviceAccountName: meshr-moderation-worker
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: moderation-worker
          image: ${EVENT_PLANE_IMAGE}
          args: ["materializer", "moderation"]
          envFrom: [{configMapRef: {name: meshr-runtime}}]
          # Keep endpoint rotation in the pod template so Flux rolls existing
          # intake workers on a changed queue configuration. Provider calls
          # belong exclusively to the screening deployment below.
          env:
            - name: MESHR_FIRESTORE_DATABASE
              value: ${MESHR_FIRESTORE_DATABASE}
            - name: MESHR_TOPOLOGY_FIRESTORE_DATABASE
              value: ${MESHR_TOPOLOGY_FIRESTORE_DATABASE}
            - name: MESHR_MODERATION_FIRESTORE_DATABASE
              value: ${MESHR_MODERATION_FIRESTORE_DATABASE}
          ports: [{name: http, containerPort: 8080}]
          readinessProbe:
            httpGet: {path: /readyz, port: http}
            timeoutSeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe: {httpGet: {path: /healthz, port: http}, initialDelaySeconds: 15}
          securityContext: {runAsNonRoot: true, allowPrivilegeEscalation: false, readOnlyRootFilesystem: true}
          resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: 500m, memory: 256Mi}}
---
# Intake only acknowledges the source event after the durable moderation inbox
# and screening-job publish both succeed. Keep its autoscaling CPU-based; the
# provider queue is consumed by the dedicated screening deployment below.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: moderation-screening-worker
  labels: {app.kubernetes.io/name: moderation-screening-worker, app.kubernetes.io/part-of: meshr}
spec:
  replicas: 1
  selector: {matchLabels: {app.kubernetes.io/name: moderation-screening-worker}}
  template:
    metadata: {labels: {app.kubernetes.io/name: moderation-screening-worker, app.kubernetes.io/part-of: meshr}}
    spec:
      serviceAccountName: meshr-moderation-screening-worker
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: moderation-screening-worker
          image: ${EVENT_PLANE_IMAGE}
          args: ["materializer", "moderation-screening"]
          envFrom: [{configMapRef: {name: meshr-runtime}}]
          env:
            - name: MESHR_FIRESTORE_DATABASE
              value: ${MESHR_FIRESTORE_DATABASE}
            - name: MESHR_TOPOLOGY_FIRESTORE_DATABASE
              value: ${MESHR_TOPOLOGY_FIRESTORE_DATABASE}
            - name: MESHR_MODERATION_FIRESTORE_DATABASE
              value: ${MESHR_MODERATION_FIRESTORE_DATABASE}
            - name: MESHR_MODERATION_ENDPOINT
              value: ${MESHR_MODERATION_ENDPOINT}
            - name: MESHR_MODERATION_HEALTHCHECK_URL
              value: ${MESHR_MODERATION_HEALTHCHECK_URL}
            - name: MESHR_MODERATION_AUTH
              value: adc
            - name: MESHR_MODERATION_TOKEN_TYPE
              value: id_token
            - name: MESHR_MODERATION_AUDIENCE
              value: ${MESHR_MODERATION_AUDIENCE}
            - name: MESHR_MODERATION_AUTHORITY_URL
              value: http://api.meshr.svc.cluster.local:8787
            - name: MESHR_MODERATION_AUTHORITY_TOKEN_FILE
              value: /var/run/secrets/meshr-moderation/moderation-authority-token
            - name: MESHR_MODERATION_CONCURRENCY
              value: "8"
          ports: [{name: http, containerPort: 8080}]
          readinessProbe:
            httpGet: {path: /readyz, port: http}
            timeoutSeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe: {httpGet: {path: /healthz, port: http}, initialDelaySeconds: 15}
          securityContext: {runAsNonRoot: true, allowPrivilegeEscalation: false, readOnlyRootFilesystem: true}
          resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: 500m, memory: 256Mi}}
          volumeMounts:
            - {name: meshr-moderation-authority-secrets, mountPath: /var/run/secrets/meshr-moderation, readOnly: true}
      volumes:
        - name: meshr-moderation-authority-secrets
          csi:
            driver: secrets-store-gke.csi.k8s.io
            readOnly: true
            volumeAttributes: {secretProviderClass: meshr-moderation-authority-secrets}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: moderation-screening-worker}
spec:
  minReplicas: 1
  maxReplicas: 3
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: moderation-screening-worker}
  metrics:
    - type: Resource
      resource: {name: cpu, target: {type: Utilization, averageUtilization: 65}}
    # This is the durable provider-work queue, not the already-acknowledged
    # moderation intake subscription. The external metric is served by the
    # pinned GKE Cloud Monitoring adapter and verified before promotion.
    - type: External
      external:
        metric:
          name: pubsub.googleapis.com|subscription|num_undelivered_messages
          selector:
            matchLabels:
              resource.labels.subscription_id: moderation-screening-worker
        target: {type: AverageValue, averageValue: "20"}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: moderation-worker}
spec:
  minReplicas: 1
  maxReplicas: 3
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: moderation-worker}
  metrics:
    - type: Resource
      resource: {name: cpu, target: {type: Utilization, averageUtilization: 65}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-worker
  labels: {app.kubernetes.io/name: audit-worker, app.kubernetes.io/part-of: meshr}
spec:
  replicas: 1
  selector: {matchLabels: {app.kubernetes.io/name: audit-worker}}
  template:
    metadata: {labels: {app.kubernetes.io/name: audit-worker, app.kubernetes.io/part-of: meshr}}
    spec:
      serviceAccountName: meshr-audit-worker
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: audit-worker
          image: ${EVENT_PLANE_IMAGE}
          args: ["materializer", "audit"]
          envFrom: [{configMapRef: {name: meshr-runtime}}]
          env:
            - name: MESHR_FIRESTORE_DATABASE
              value: ${MESHR_FIRESTORE_DATABASE}
            - name: MESHR_TOPOLOGY_FIRESTORE_DATABASE
              value: ${MESHR_TOPOLOGY_FIRESTORE_DATABASE}
            - name: MESHR_EVENT_AUDIT_FIRESTORE_DATABASE
              value: ${MESHR_EVENT_AUDIT_FIRESTORE_DATABASE}
          ports: [{name: http, containerPort: 8080}]
          readinessProbe: {httpGet: {path: /readyz, port: http}}
          livenessProbe: {httpGet: {path: /healthz, port: http}, initialDelaySeconds: 15}
          securityContext: {runAsNonRoot: true, allowPrivilegeEscalation: false, readOnlyRootFilesystem: true}
          resources: {requests: {cpu: 75m, memory: 96Mi}, limits: {cpu: 300m, memory: 192Mi}}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: audit-worker}
spec:
  minReplicas: 1
  maxReplicas: 3
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: audit-worker}
  metrics:
    - type: Resource
      resource: {name: cpu, target: {type: Utilization, averageUtilization: 65}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-worker
  labels: {app.kubernetes.io/name: notification-worker, app.kubernetes.io/part-of: meshr}
spec:
  replicas: 1
  selector: {matchLabels: {app.kubernetes.io/name: notification-worker}}
  template:
    metadata: {labels: {app.kubernetes.io/name: notification-worker, app.kubernetes.io/part-of: meshr}}
    spec:
      serviceAccountName: meshr-notification-worker
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: notification-worker
          image: ${EVENT_PLANE_IMAGE}
          args: ["materializer", "notifications"]
          envFrom: [{configMapRef: {name: meshr-runtime}}]
          env:
            - name: MESHR_FIRESTORE_DATABASE
              value: ${MESHR_FIRESTORE_DATABASE}
            - name: MESHR_TOPOLOGY_FIRESTORE_DATABASE
              value: ${MESHR_TOPOLOGY_FIRESTORE_DATABASE}
            - name: MESHR_NOTIFICATIONS_FIRESTORE_DATABASE
              value: ${MESHR_NOTIFICATIONS_FIRESTORE_DATABASE}
          ports: [{name: http, containerPort: 8080}]
          readinessProbe: {httpGet: {path: /readyz, port: http}}
          livenessProbe: {httpGet: {path: /healthz, port: http}, initialDelaySeconds: 15}
          securityContext: {runAsNonRoot: true, allowPrivilegeEscalation: false, readOnlyRootFilesystem: true}
          resources: {requests: {cpu: 75m, memory: 96Mi}, limits: {cpu: 300m, memory: 192Mi}}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: notification-worker}
spec:
  minReplicas: 1
  maxReplicas: 3
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: notification-worker}
  metrics:
    - type: Resource
      resource: {name: cpu, target: {type: Utilization, averageUtilization: 65}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: live-gateway
  labels: {app.kubernetes.io/name: live-gateway, app.kubernetes.io/part-of: meshr}
spec:
  replicas: 2
  selector: {matchLabels: {app.kubernetes.io/name: live-gateway}}
  template:
    metadata: {labels: {app.kubernetes.io/name: live-gateway, app.kubernetes.io/part-of: meshr}}
    spec:
      serviceAccountName: meshr-live-gateway
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: live-gateway
          image: ${EVENT_PLANE_IMAGE}
          args: ["live-gateway"]
          envFrom: [{configMapRef: {name: meshr-runtime}}]
          env:
            - {name: MESHR_PORT, value: "8080"}
            - name: MESHR_FIRESTORE_DATABASE
              value: ${MESHR_FIRESTORE_DATABASE}
            - name: MESHR_TOPOLOGY_FIRESTORE_DATABASE
              value: ${MESHR_TOPOLOGY_FIRESTORE_DATABASE}
            # Explicitly templated so cost-protection changes roll the live
            # gateway instead of leaving an old fan-out mode running.
            - name: MESHR_COST_PROTECTION_MODE
              value: ${MESHR_COST_PROTECTION_MODE}
          ports: [{name: http, containerPort: 8080}]
          readinessProbe: {httpGet: {path: /readyz, port: http}}
          livenessProbe: {httpGet: {path: /healthz, port: http}, initialDelaySeconds: 15}
          securityContext: {runAsNonRoot: true, allowPrivilegeEscalation: false, readOnlyRootFilesystem: true}
          resources: {requests: {cpu: 150m, memory: 192Mi}, limits: {cpu: "1", memory: 512Mi}}
---
apiVersion: v1
kind: Service
metadata: {name: live-gateway}
spec:
  selector: {app.kubernetes.io/name: live-gateway}
  ports: [{name: http, port: 8080, targetPort: http}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: {name: live-gateway}
spec:
  minAvailable: 1
  selector: {matchLabels: {app.kubernetes.io/name: live-gateway}}
